<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing consume history for <%= @consume_history.inventory.product.name %></h1>

  <%= render "form", inventory: nil, consume_history: @consume_history %>

  <%= link_to "Back", inventory_path(@consume_history.inventory), class: "btn-secondary block w-full sm:w-fit mt-2 text-center" %>
</div>
