<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing storage</h1>

  <%= render "form", storage: @storage %>

  <div class="flex flex-col sm:flex-row mt-2">
    <%= link_to "Show this storage", @storage, class: "btn-secondary block mb-2 sm:mb-0 text-center" %>
    <%= link_to "Back to storages", storages_path, class: "btn-secondary block sm:ml-2 text-center" %>
  </div>
</div>
