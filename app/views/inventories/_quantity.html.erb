<% if inventory.total_consumption < inventory.quantity %>
  <%= format_number(inventory.quantity - inventory.total_consumption) %>
  /
<% end %>
<%= format_number(inventory.quantity) %>
<%= inventory.unit.name %>
