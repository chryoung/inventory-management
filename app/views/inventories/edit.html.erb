<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing inventory</h1>

  <%= render "form", inventory: @inventory %>

  <div class="flex flex-col sm:flex-row mt-2">
    <%= link_to "Show this inventory", @inventory, class: "btn-secondary block mb-2 sm:mb-0 text-center" %>
    <%= link_to "Back to inventories", inventories_path, class: "btn-secondary block sm:ml-2 text-center" %>
  </div>
</div>
