<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New inventory</h1>

  <%= render "form", inventory: @inventory %>

  <div class="flex flex-col sm:flex-row mt-2">
    <%= link_to 'Back to inventories', inventories_path(@inventory), class: "btn-secondary block text-center" %>
  </div>
</div>
