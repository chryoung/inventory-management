<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing item</h1>

  <%= render "form", item: @item %>

  <div class="flex flex-col sm:flex-row sm:mt-2">
    <%= link_to "Show this item", @item, class: "btn-secondary block mb-2 sm:mb-0 text-center" %>
    <%= link_to "Back to items", items_path, class: "btn-secondary block sm:ml-2 text-center" %>
  </div>
</div>
