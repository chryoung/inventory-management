<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing unit</h1>

  <%= render "form", unit: @unit %>

  <div class="flex flex-col sm:flex-row">
    <%= link_to "Show this unit", @unit, class: "btn-secondary block mb-2 sm:mb-0 text-center" %>
    <%= link_to "Back to units", units_path, class: "btn-secondary block sm:ml-2 text-center" %>
  </div>
</div>
