<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <% cache @unit do %>
      <%= render @unit %>
    <% end %>

    <div class="flex flex-col sm:flex-row">
      <%= link_to 'Edit this unit', edit_unit_path(@unit), class: "btn-primary block w-full sm:w-fit" %>
      <%= button_to 'Destroy this unit', unit_path(@unit), method: :delete, class: "btn-secondary text-left block w-full sm:mx-2 my-2 sm:my-0 sm:w-fit" %>
      <%= link_to 'Back to units', units_path, class: "btn-secondary block w-full sm:w-fit" %>
    </div>
  </div>
</div>
