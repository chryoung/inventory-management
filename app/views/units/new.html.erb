<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New unit</h1>

  <%= render "form", unit: @unit %>

  <div class="flex flex-col sm:flex-row mt-2">
    <%= link_to 'Back to units', units_path, class: "btn-secondary text-center" %>
  </div>
</div>
